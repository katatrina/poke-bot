server:
  port: 8080

qdrant:
  host: "localhost"
  port: 6334
  collection: "pokemons"

ollama:
  base_url: "http://localhost:11434"
  chat_model: "qwen2.5-coder:3b"
  embedding_model: "nomic-embed-text"

rag:
  chunk_size: 600
  chunk_overlap: 100
  top_k: 5
  temperature: 0.3
  max_conversation_turns: 15    # Max 15 turns (30 messages)
  max_total_characters: 10000   # ~2500 tokens total
